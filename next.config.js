/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
      domains: ['images.unsplash.com', 'dkmdfyxxcztjqusyfipv.supabase.co']
  }
}

module.exports = nextConfig
